logging:
  config: classpath:logback-spring.xml    
  
spring:
  profiles:
    active: test
  datasource:
     driverClassName: org.h2.Driver
     url: jdbc:h2:file:~/routing_test
     username: sa
     password:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
    